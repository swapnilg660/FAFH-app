import React from "react";
import { ScrollView } from "native-base";

function CapturedMeals({ navigation, route }) {
  const { meals, occasion } = route.params;

  return <ScrollView></ScrollView>;
}

export default CapturedMeals;
